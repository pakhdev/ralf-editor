export enum MutationType {
    NODE_INSERTION = 'nodeInsertion',
    NODE_DELETION = 'nodeDeletion',
    TEXT_INSERTION = 'textInsertion',
    TEXT_DELETION = 'textDeletion',
    TEXT_SPLITTING = 'textSplitting',
    TEXT_MERGING = 'textMerging',
}
